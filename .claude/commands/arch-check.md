---
description: Run architecture check on changed files or a named module
argument-hint: [file-or-module]
---

Use the `arch-check` skill on: $ARGUMENTS

If no argument provided, check all files changed since the last commit using `git diff --name-only HEAD`.
