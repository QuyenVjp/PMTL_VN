# Example Outline

Use this as a minimal folder target for a new canonical skill:

```text
skill-name/
  SKILL.md
  templates/
  verification/
  examples/
  gotchas.md
  changelog.md
```

Add `scripts/` when the workflow includes repeatable commands or data transformations.
