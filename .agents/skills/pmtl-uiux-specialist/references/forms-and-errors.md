# Forms and Errors

## Required behavior

- labels above inputs
- inline errors near the failing field
- focus the first invalid field after submit
- visible pending state on submit
- success state should confirm what changed, not just disappear silently

## Avoid

- placeholder-only labeling
- toast-only validation
- generic “something went wrong” when the field-level problem is known
