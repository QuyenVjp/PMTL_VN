# Gotchas

- Jumping straight to a global skill often skips PMTL repo policy and causes drift.
- Using a compatibility alias as the default target makes routing inconsistent for future sessions.
- Superpowers is strong for workflow control, but it should not become the place where PMTL-specific rules live.

