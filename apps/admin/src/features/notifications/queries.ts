/**
 * Notifications query factories — re-exported from @pmtl/api-client.
 * Source of truth: packages/api-client/src/admin/notifications.ts
 */
export {
  pushKeys,
  pushJobListOptions,
  pushStatusOptions,
  subscriptionStatsOptions,
} from "@pmtl/api-client";

export type { PushJobItem, PushStatus, SubscriptionStats, PushJobFilters } from "@pmtl/api-client";
